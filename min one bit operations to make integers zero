class Solution:
    def minimumOneBitOperations(self, n: int) -> int:
        def helper(x):
            if x == 0:
                return 0
            k = x.bit_length() - 1
            return (1 << (k + 1)) - 1 - helper(x ^ (1 << k))
        return helper(n)
